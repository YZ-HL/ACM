#include <bits/stdc++.h>
#define maxn 100005
using namespace std;
int t, n, m;
int main(void)
{
    scanf("%d", &t);
    while(t--)
    {
        scanf("%d %d", &n, &m);
        printf("%d\n", n * m);
    }
    return 0;
}
